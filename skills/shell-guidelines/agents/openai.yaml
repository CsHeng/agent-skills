interface:
  display_name: "Shell Guidelines"
  short_description: "Review persisted or correctness-sensitive Shell code"
  default_prompt: "Use $shell-guidelines as a conditional policy overlay for this non-trivial Shell surface."
policy:
  allow_implicit_invocation: true
