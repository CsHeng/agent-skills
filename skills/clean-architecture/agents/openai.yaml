interface:
  display_name: "Clean Architecture (Compatibility)"
  short_description: "Explicit handoff to architecture-patterns"
  default_prompt: "Use $clean-architecture only as an explicit compatibility handoff to $architecture-patterns."
policy:
  allow_implicit_invocation: false
