interface:
  display_name: "Development Standards"
  short_description: "Overlay scoped cross-language implementation policy"
  default_prompt: "Use $development-standards as a conditional overlay for repository-owned implementation and quality policy."
policy:
  allow_implicit_invocation: true
