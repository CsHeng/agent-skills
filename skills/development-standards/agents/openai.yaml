interface:
  display_name: "Development Standards"
  short_description: "Keep implementations small, durable, and scoped"
  default_prompt: "Use $development-standards to apply the repo's cross-language implementation, dependency, and compatibility policy."
