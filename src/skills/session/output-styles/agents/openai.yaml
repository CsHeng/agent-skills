interface:
  display_name: "Output Styles"
  short_description: "Shared non-primary rendering baseline for coding work"
  default_prompt: "Apply the shared engineering rendering baseline after selecting the primary skill."
