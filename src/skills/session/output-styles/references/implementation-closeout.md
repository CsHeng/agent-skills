# Implementation Closeout

Use after completing edits, verification, deployment, install, or cleanup work.

## Required Content

- Whether the main goal and requested endpoint completed.
- What changed, including the actual delivered candidate or version.
- Authorized secondary tradeoffs with reason and impact, when any exist.
- Verification run and pass/fail status.
- Anything not run, not verified, or not completed.
- Follow-up only when it is directly useful and grounded in the request.

If the closeout needs multiple item groups, use globally unique labels instead of restarting numbered lists:

- changed items: `C1`, `C2`
- verification items: `V1`, `V2`
- residual risks or follow-ups: `R1`, `R2`

## Rules

- Do not claim completion before the requested write, install, deploy, or verification command has succeeded.
- Do not claim every feature complete when only the main goal and approved acceptance were met.
- Keep the closeout scoped to touched files and observed behavior.
- Avoid long implementation narratives.
- If memory was used and the active environment requires memory citation, include the required citation block outside generated artifacts.
