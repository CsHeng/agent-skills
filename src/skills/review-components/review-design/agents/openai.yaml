interface:
  display_name: "Review Design"
  short_description: "Controller-only design candidate-finding evaluator"
  default_prompt: "Use $review-design only when review-change supplies a bounded design review brief."
