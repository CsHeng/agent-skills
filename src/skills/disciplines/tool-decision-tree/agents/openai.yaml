interface:
  display_name: "Tool Decision Tree"
  short_description: "Select non-trivial ad hoc tools and safe command flows"
  default_prompt: "Use $tool-decision-tree when this task needs a non-trivial ad hoc tool or command-composition decision."
