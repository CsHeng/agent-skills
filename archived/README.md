# Archived Material

This tree preserves historical project material. It is not part of active plugin discovery, installation, routing, or validation.

`commands/` contains retired Claude Code command wrappers whose durable behavior now belongs to public skills. Use `rg --no-ignore archived/commands` only when historical command context is explicitly required.
